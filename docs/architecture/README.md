# SiteFueler — System Architecture

This folder documents **how SiteFueler works** (system behavior), kept separate
from **how it looks** (`../design-system.md` and `../components/`).

| Documentation type  | Location                  | Answers                          |
| ------------------- | ------------------------- | -------------------------------- |
| Design system       | `../design-system.md`     | Global visual rules / tokens     |
| Component specs      | `../components/`          | Exact behavior of each component |
| System architecture | `./` (this folder)        | How the application is built     |

## Planned documents

- `routing.md` — URL structure and route organization
- `database.md` — schema, relationships, migrations
- `authentication.md` — auth flows and access control
- `storage.md` — file storage strategy
- `media.md` — image/asset handling (WebP, ratios, optimization)
- `admin-panel.md` — admin architecture

These are placeholders for now and will be filled in as each system is built.
